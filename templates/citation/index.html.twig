{% extends 'base.html.twig' %}

{% block title %}Hello DouController!{% endblock %}

{% block body %}
<style>
        /* @import url('https://fonts.googleapis.com/css2?family=Amiri&display=swap'); */

    .rrr{
      font-family: 'Amiri', serif;
      /* direction: rtl; */
      text-align: center;
      font-size: 24px;
      line-height: 1.8;
      border:10px black solid;
    }
    .textt{
      font-family: 'Amiri', serif;
      font-size: 24px;
      line-height: 1.8;
    }
</style>
{# <div class="bg-red w-100 border"> #}
    <div class=" bg-light  mt-4  "><br>
        {# {{ dump(doua) }} #}
        
            {% for Doua in citation %}
                <div class="card w-50 mt-2 text-center border-success m-auto">
                    <div class="card-body">
                        <h2 class="card-title  text-uppercase small textt ">CITATION</h2>
                        <h3 class="card-text"> {{ Doua.contenuOriginal }} </h3><br>
                        {# <h4 class="">Traduction en francais</h4> #}
                        <p class="card-text textt"> {{ Doua.contenuTraduit }} </p>
                    </div>
                    <div class="card-footer">
                    <h6> Auteur : {{ Doua.auteur }} </h6>
                    </div>
                </div>

            {% endfor %}
        </div>  
     {# </div> #}
{% endblock %}
